# icontext-feature — OpenAI Codex CLI adapter

> Follow `PROCEDURE.md` to create one feature plan: next `plans/NNN`, gather brief, optional
> persona panel, write `plans/NNN-<slug>.md` from `templates/plan-template.md` (status `todo`,
> ref CONTEXT.md), register in `plans/README.md` + `PLAN.md`. Stop at the plan. Don't commit.

Codex auto-reads the project's `AGENT.md`/`AGENTS.md`, so project rules load automatically.
Tool mapping: questions → inline · file ops → shell/file.
