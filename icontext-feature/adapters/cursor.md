# icontext-feature — Cursor adapter

> In chat: read `PROCEDURE.md` and execute it to create one feature plan. Next `plans/NNN`,
> ask the brief, ask about the persona panel, write `plans/NNN-<slug>/ (folder, role files)` from
> `templates/plan-template/` (status `todo`, ref CONTEXT.md), register in `plans/README.md`
> and `PLAN.md`. Stop at the plan; don't implement; don't commit.

Tool mapping: questions → inline chat · file ops → terminal/editor.
