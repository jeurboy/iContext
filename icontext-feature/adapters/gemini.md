# icontext-feature — Gemini CLI adapter

> Read `PROCEDURE.md` in this folder and execute it to create one feature plan.
> Find the next `plans/NNN`, ask me the minimal brief, ask me to choose `/grill-with-docs` or
> `/wayfinder` for requirement discovery, run the selected route, review requirements through
> `ROLE.md` as a multi-agent panel, write
> `plans/NNN-<slug>/ (folder, role files)` from `templates/plan-template/` (task status `plan`, ref CONTEXT.md),
> then register it in `plans/README.md` and `PLAN.md`, and run `/autoplan` or the equivalent review
> chain before implementation. Stop at the plan; don't implement.

Tool mapping: questions → ask inline · file ops → shell/file tools · don't commit.
