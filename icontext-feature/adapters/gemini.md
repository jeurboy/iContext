# icontext-feature — Gemini CLI adapter

> Read `PROCEDURE.md` in this folder and execute it to create one feature plan.
> Find the next `plans/NNN`, ask me the brief, ask whether to run the persona panel, write
> `plans/NNN-<slug>/ (folder, role files)` from `templates/plan-template/` (task status `plan`, ref CONTEXT.md),
> then register it in `plans/README.md` and `PLAN.md`. Stop at the plan; don't implement.

Tool mapping: questions → ask inline · file ops → shell/file tools · don't commit.
